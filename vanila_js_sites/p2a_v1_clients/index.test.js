// tests/main.test.js
import { createPost } from '../static/js/main.js';

describe('Blog Post Creation', () => {
  test('should create a post object with the correct title and content', () => {
    expect(1==1).toBe(true)
    // const post = createPost('New Post', 'This is the content of the new post');
    // expect(post.title).toBe('New Post');
    // expect(post.content).toBe('This is the content of the new post');
    // expect(post).toHaveProperty('createdAt');
  });
});
